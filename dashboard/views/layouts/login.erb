<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1">
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <title><%= @title || "Survey in Pocket - Dashboard" %></title>
  <meta name="description" content="<%= @description %>">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="author" content="Survey in Pocket">
  <meta name="contact" content="info@surveyinpocket.com">
  <meta name="copyright" content="&copy; 2014 surveyinpocket.com">
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
  <%= stylesheet_link_tag '/assets/application' %>

</head>
<body>
    <header>
      <script type="text/javascript">
        var lang = '<%= session["lang"] || 'en' %>'
        $(document).ready(function(){
          if(lang){
            $("#language").val(lang);
          }
          $("#language").change(function(){
            window.location = "?lang=" + $(this).val();
          });
        });
      </script>

      <select id="language" style="float: right; margin: 15px">
        <option value="en">
          English
        </option>
        <option value="vi">
          Tiếng Việt
        </option>
      </select>
    </header>
    <%= yield %>
</body>
</html>
